  <div class="d-flex justify-content-between logo-letruc logopos">
    <%= image_tag "logo.svg", alt: "Le Wagon", width: 65 %>
    <h1>Le Truc</h1>
  </div>


<div class="d-flex justify-content-between ">

<div class="d-flex mx-auto">
<%= render "components/landing", mudar: "spandois"%>
</div>


<div class="d-flex align-items-center justify-content-center regista-div flex-column animate__animated animate__slideInRight">

<a class="arrow align-self-start ms-2 p-2" href="/"><i class="fa-solid fa-arrow-right display-6" style="color: #ffffff;"></i></a>

<div class="glasscard d-flex flex-column">
<h2 class="text-center display-6 pb-3">Sign in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions btn-red mt-5">
    <%= f.button :submit, "Sign in"%>
  </div>

<% end %>

 </div>
 </div>

 </div>
